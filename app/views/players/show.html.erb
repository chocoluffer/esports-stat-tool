<h2><%= @player.name %></h2>
 
<p>
  <strong>Main Heros:</strong>
  <%= @player.mains %>
</p>
<hr>
<h2>Comments</h2>
<%= render @player.comments %>

<h3>Leave a comment:</h3>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_player_path(@player) %> | 
<%= link_to 'Back', players_path %>